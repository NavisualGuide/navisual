.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri
src-tauri/target/

# Runtime data (sessions, usage logs) — user-specific, not source
src-tauri/.ai_navigator/

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv*/
.pytest_cache/
htmlcov/
.coverage

# IDE / local
.vscode/
.claude/

# Internal — goes to NavisualGuide/navisual-internal (private repo)
# Push workflow: see REPOS.md in navisual-internal, or run:
#   git fetch internal && git checkout -b internal-push internal/main
#   git add -f docs/ supabase/ && git commit && git push internal internal-push:main
#   git checkout main && git branch -d internal-push
docs/
supabase/

# Build artifacts
dist/

# Spike test output
tools/spike_output/


# Private test fixtures
fixtures/
