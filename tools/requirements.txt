# Runtime deps for tools/bbox_spike.py and friends.
# Install with: pip install -r tools/requirements.txt
#
# Note: anthropic / google-genai are listed for completeness — the spike runner
# calls both APIs via httpx directly (same pattern as legacy/ClientS), so the
# official SDKs are not strictly required. Install them if you want to poke at
# the APIs interactively in a REPL.

# Core image + HTTP
Pillow>=10.2
httpx>=0.27
python-dotenv>=1.0

# Official SDKs (optional for the script itself, useful for ad-hoc testing)
anthropic>=0.39
google-genai>=0.3

# Windows.Media.Ocr backend — Windows-only.
# winrt-runtime pulls in the base projection; Media.Ocr + Graphics.Imaging +
# Storage.Streams are the three namespaces used by the OCR code.
winrt-runtime>=2.0 ; sys_platform == "win32"
winrt-Windows.Foundation>=2.0 ; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=2.0 ; sys_platform == "win32"
winrt-Windows.Media.Ocr>=2.0 ; sys_platform == "win32"
winrt-Windows.Graphics.Imaging>=2.0 ; sys_platform == "win32"
winrt-Windows.Storage.Streams>=2.0 ; sys_platform == "win32"
